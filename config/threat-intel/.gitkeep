# This directory holds threat intelligence feed files
# ip-blocklist.yml   — IP addresses to block/alert on
# domain-blocklist.yml — Malicious domains
# url-blocklist.yml  — Malicious URLs
# These files are generated by SIEMBA when you upload threat intel feeds in the UI
# They are excluded from git via .gitignore
